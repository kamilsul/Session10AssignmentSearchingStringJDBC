*This is a project to that uses the normal JDBC to get user details from the DB and search for a user whose name contains the given search text. 
*This is a basic servlet project with JDBC.
*This project doesn't use ORM tools or frameworks such as Hibernate and Spring, deliberately because it's a simple excercise to lay the groundwork for the advanced topics.
